function geradorDeTagsDeIdentificacao(nome) {
  return nome.toUpperCase()
}
function verificarSePodeSerAdotado(idade, tamanho ){
  if (idade===1) {
    return (tamanho === "M")? "Pode ser adotado"
    :"Tamanho não permitido pra adoção"   
  }else{
    "Infelizmente a idade de adoção tem que ser de 1 ano "
  }
  
}
function calcularConsumoDeRacao(params) {
  
}
function decidirTipoDeAtividadePorPorte(params) {
  
}
function buscarDadoAsync(params) {
  
}